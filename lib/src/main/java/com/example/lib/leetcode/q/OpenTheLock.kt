package com.example.lib.leetcode.q

class OpenTheLock {

    fun openLock(deadends: Array<String>, target: String): Int {

        // find the digits which needs to be changed
        // worst case all of them needs to be changed
        return 0
    }
}